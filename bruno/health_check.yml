info:
  name: health_check
  type: http
  seq: 32

http:
  method: GET
  url: http://127.0.0.1:8000/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
