info:
  name: get_record_by_id
  type: http
  seq: 18

http:
  method: GET
  url: http://127.0.0.1:8000/records/2
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
