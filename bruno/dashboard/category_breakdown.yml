info:
  name: category_breakdown
  type: http
  seq: 26

http:
  method: GET
  url: http://127.0.0.1:8000/dashboard/category-breakdown
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
