info:
  name: monthly_trends
  type: http
  seq: 28

http:
  method: GET
  url: http://127.0.0.1:8000/dashboard/monthly-trends
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
