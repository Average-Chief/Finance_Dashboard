info:
  name: recent_records
  type: http
  seq: 30

http:
  method: GET
  url: http://127.0.0.1:8000/dashboard/recent-activity?limit=10
  params:
    - name: limit
      value: "10"
      type: query
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
