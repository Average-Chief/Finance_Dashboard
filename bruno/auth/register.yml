info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/auth/register
  body:
    type: json
    data: |-
      {
        "email": "user@finance.com",
        "name": "Jacob Black",
        "password": "users123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
