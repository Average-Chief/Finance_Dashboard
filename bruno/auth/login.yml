info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/auth/login
  body:
    type: json
    data: |-
      {
        "email":"user@finance.com",
        "password": "users123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
