info:
  name: my_profile
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:8000/auth/me
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
