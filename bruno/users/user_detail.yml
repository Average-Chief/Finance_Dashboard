info:
  name: user_detail
  type: http
  seq: 8

http:
  method: GET
  url: http://127.0.0.1:8000/users/1
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
