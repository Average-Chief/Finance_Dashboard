info:
  name: users_list
  type: http
  seq: 6

http:
  method: GET
  url: http://127.0.0.1:8000/users/
  auth:
    type: bearer
    token: "{{Bearer}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
